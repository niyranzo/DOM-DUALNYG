import divForm from "./components/divForm.js";

/*
crear un formulario que permuta 
validar un correo electrónico, si
 el email no es válido mostrara un
 mensaje de error dinámico bajo el campo de texto
*/

/*
dado un array de obj, con datos de usuarios:
(nombre, edad, email) genera una tabla dinamicamente
en la que cada fila corresponda a un usuario.
Añade funcionalidad para ordenar las filas
por nombre o edad haciendo click en los encabezados
--
utilizando la lista de usuarios añadir un input para que 
a medida q busquemos, filtre los usuarios por su nombre.
<table>
  <tr>
    <th>Company</th>
    <th>Contact</th>
    <th>Country</th>
  </tr>
  <tr>
    <td>Alfreds Futterkiste</td>
    <td>Maria Anders</td>
    <td>Germany</td>
  </tr>
  <tr>
    <td>Centro comercial Moctezuma</td>
    <td>Francisco Chang</td>
    <td>Mexico</td>
  </tr>
</table>
 
*/
document.addEventListener("DOMContentLoaded", function () {
    const app = document.getElementById("app");
    // const divFormulario = divForm();
    // app.appendChild(divFormulario);
});